{...}: {
  programs = {
    vscode = {
      enable = true;
      # let vscode sycnc and update its configurations & extensions across devices, using github account
      userSettings = {};
    };
  };
}
